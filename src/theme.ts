import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#ea2b06',
    },
    secondary: {
      main: '#434343',
    },
    background: {
      default: '#f8f8f8',
    },
  },
})
