import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#ea2b06',
    },
    secondary: {
      main: '#525252',
    },
    background: {
      default: '#e0e0e0',
    },
  },
})
